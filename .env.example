APP_NAME=Ceylon Eats
DATABASE_URL=postgresql://ceylon:local_dev_only@127.0.0.1:55432/ceylon_eats
PORT=3001
APP_ORIGIN=http://127.0.0.1:5173
# Set NODE_ENV=production in the deployment environment when using HTTPS.
# Set a private password of at least 12 characters before running db:seed.
SEED_PASSWORD=
